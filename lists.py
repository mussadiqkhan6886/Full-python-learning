students = ["Harry", "Rob", "Hermione"]

for i in range(len(students)): # It will iterate number of times the len of list 
    print(i + 1, students[i] ) # it will print one element of students list each time it iterate by index i